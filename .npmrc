# Expo Router/SDK 56이 선언한 선택적 peer(@testing-library/dom,
# react-native-reanimated/worklets)를 사용하지 않는 프로젝트다. npm이 이 선택적 peer를
# 필수처럼 자동 해석해 npm ci가 실패하지 않도록 레거시 peer 해석을 유지한다.
# 해당 패키지를 실제 도입하거나 Expo SDK를 올릴 때 제거 가능 여부를 다시 확인한다.
legacy-peer-deps=true
